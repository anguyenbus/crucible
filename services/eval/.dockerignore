# Keep local-only code, tests, and build artifacts OUT of the image.
# Only app/ (+ pyproject.toml/uv.lock for the build) ship.

# Local-only dev zone (ChromaDB stub + eval-rag CLI + fixtures) -- NEVER ships.
dev/

# Tests are not part of the runtime image.
tests/

# Resolved virtualenvs / build outputs.
.venv/
dist/
build/
*.egg-info/

# Python + tooling caches.
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.import_linter_cache/

# Scratch / scaffolding scripts not needed at runtime.
scripts/
