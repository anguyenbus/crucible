# Pinned pipeline config: legal-rag-default 1.0.0
#
# IMMUTABLE once released — never edit this file. Any change requires a NEW
# {name}-{semver} file plus a new entry in manifest.sha256 (enforced by the
# immutability test). Values are Phase-2 compatible with the Bedrock-only
# constraint (au.* inference profiles, ap-southeast-2); Phase 1 makes NO
# boto3 calls — these are pins only.
name: legal-rag-default
version: 1.0.0
region: ap-southeast-2
generator:
  model_id: au.anthropic.claude-sonnet-4-6
  temperature: 0.0
embedder:
  model_id: amazon.titan-embed-text-v2:0
prompt_template:
  ref: legal-rag-answer
  version: 1.0.0
retrieval:
  top_k: 8
  params:
    search_type: hybrid
guardrails:
  policy_version: 0.0.0
